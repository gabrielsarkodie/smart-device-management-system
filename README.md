# smart-device-management-system
This is a simple program I built for my OOP class. It manages smart home devices using Python.

What it does:

It uses classes to represent devices like sensors, lights, and cameras.

It follows OOP principles like inheritance (sharing code) and encapsulation (keeping data safe).

It has a simple menu where you can turn devices on/off, check temps, or change brightness.
